package com.ysteimle.segproject.easywalkin;

interface AppUser {
    // Interface implemented by all users of the app (admin, patient, clinic employee)
    // might be removed later if proves to be unnecessary
}